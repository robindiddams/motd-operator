---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: motds.motd.motd.dev
spec:
  group: motd.motd.dev
  names:
    kind: Motd
    listKind: MotdList
    plural: motds
    singular: motd
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Motd is the Schema for the motds API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MotdSpec defines the desired state of Motd.
            properties:
              intensity:
                description: 'Intensity controls pod behavior: "" for stable, "crashloop"
                  for crashing pods (shows red in k9s)'
                type: string
              message:
                description: Message is the text to display via pod names
                type: string
            required:
            - message
            type: object
          status:
            description: MotdStatus defines the observed state of Motd.
            properties:
              podNames:
                description: PodNames contains the names of pods currently displaying
                  the message
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
